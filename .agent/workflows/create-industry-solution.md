---
description: "从行业资料/文档生成行业解决方案页面"
---

# 行业解决方案页面生成工作流

从 PPTX/PDF/链接 生成行业解决方案页面。

---

## Step 1: 提取内容

| 素材 | 命令 |
|------|------|
| **PPTX** | `pptx2md "file.pptx" -o /tmp/content.md --disable-image` |
| **PDF** | `pdftotext "file.pdf" /tmp/content.txt` |
| **链接** | 用 `read_url_content` 工具 |

// turbo
```bash
pptx2md "$PPTX_PATH" -o /tmp/industry-content.md --disable-image
```

---

## Step 2: 创建目录

// turbo
```bash
mkdir -p src/app/\(frontend\)/solution/industry/[slug]
```

---

## Step 3: 生成组件

| 文件 | 用途 |
|------|------|
| `page.tsx` | 入口 + SEO |
| `Hero.tsx` | 首屏（左文字60% + 右图标40%） |
| `IndustryChallenges.tsx` | 行业挑战（3-4个） |
| `SolutionOverview.tsx` | 方案总览 |
| `KeyScenarios.tsx` | 核心场景（Tab切换） |
| `IndustryCases.tsx` | 行业案例（2-3个） |
| `ValueSection.tsx` | 价值指标 |
| `CTASection.tsx` | 底部CTA |

---

## Step 4: Hero Banner 规范

**布局**：左文字(60%) + 右图标(40%)

**右侧配图优先级**：
1. 🥇 **Lucide 图标组合** - 推荐，~1KB
2. 🥈 **CSS 渐变 + 图标** - 0KB
3. 🥉 **SVG 文件** - <50KB
4. ❌ **PNG/JPG 大图** - 禁止

**行业图标映射**：
| 行业 | 图标 |
|------|------|
| 制造业 | `Factory` |
| 零售 | `ShoppingBag` |
| 医药 | `Pill` |
| 化工 | `Beaker` |
| 乳制品 | `Milk` |
| 酒类 | `Wine` |
| 餐饮 | `UtensilsCrossed` |
| 半导体 | `Cpu` |
| 互联网 | `Globe` |
| 国企 | `Building2` |

---

## Step 5: 更新行业分类（多端同步）

> [!IMPORTANT]
> 为了确保新行业能够显示在 **导航栏 (Mega Menu)** 和 **解决方案中心页面**，必须更新 `src/data/solutions.ts`。

### 5.1 确定行业大类

检查新行业是否适配以下现有 **8 大类**：

| 大类 | 适用行业示例 |
|------|--------------|
| **芯片半导体** | 芯片、电子IC、集成电路 |
| **高端制造** | 新材料、电气装备、汽车、家电、汽配、电子组装 |
| **能源化工** | 能源、基础化工、石化 |
| **医药与大健康** | 医药、医疗器械、生物制药、CDMO、中药 |
| **消费品与餐饮** | 白酒、乳制品、餐饮、化妆品、啤酒、食品、休食、服装 |
| **现代服务业** | 科技服务、商务服务、互联网、专业服务 |
| **工程与基建** | 工程项目管理、建筑 |
| **特色专题** | 中企出海、国资国企 |

### 5.2 更新 `solutionByIndustryCategory` (核心)

这是 UI 显示的最重要来源。在 `solutionByIndustryCategory` 中找到对应大类，添加新项：

```tsx
// 找到对应的大类，如：
{
  name: '医药与大健康',
  items: [
    // ...existing items
    { 
       label: '[行业名称]', 
       href: '/solution/industry/[slug]', 
       desc: '[一句话描述]', 
       icon: [IndustryIcon] 
    },
  ]
},
```

### 5.3 更新 `solutionByIndustry` (兼容/脚本)

为了兼容旧逻辑和后台脚本（如 `updateHeader.ts`），也必须在扁平数组中同步：

```tsx
{
  name: '[行业名称]',
  icon: [IndustryIcon],
  href: '/solution/industry/[slug]',
  desc: '[一句话描述]',
},
```

---

## Step 6: 验证（确保全站透出）

// turbo
```bash
npm run lint
```

**必须检查以下两个显示位置**：

1.  **导航栏 (Navbar Mega Menu)**：
    -   鼠标悬浮「解决方案」 -> 切换到「按行业」标签页。
    -   检查是否在对应的行业大类下（如「高端制造」）看到了新行业条目。
2.  **解决方案中心页面 (`/solution`)**：
    -   点击「按行业场景」切换按钮。
    -   检查新行业是否以卡片形式显示在正确的大类区域中。

**其他检查项**：
- [ ] 页面可访问 `/solution/industry/[slug]`
- [ ] SEO: title/description 包含行业名
- [ ] 顶部面包屑（如有）路径正确

---

## Step 7: 组件完整性检查 ⚠️ 必做

**执行以下检查，确保所有组件符合规范：**

### 7.1 文件完整性检查

// turbo
```bash
ls -la src/app/\(frontend\)/solution/industry/[slug]/
```

**必须包含 8 个文件**：
- [ ] `page.tsx`
- [ ] `Hero.tsx`
- [ ] `IndustryChallenges.tsx`
- [ ] `SolutionOverview.tsx`
- [ ] `KeyScenarios.tsx`
- [ ] `IndustryCases.tsx`
- [ ] `ValueSection.tsx`
- [ ] `CTASection.tsx`

### 7.2 Hero.tsx 布局检查 🎯

**Hero 必须包含以下结构**：

```tsx
// ✅ 正确结构
<div className="flex flex-col lg:flex-row items-center gap-12 lg:gap-20">
  {/* 左侧: 文字内容 (60%) */}
  <div className="flex-1 text-center lg:text-left">
    {/* 行业标签 + 标题 + 副标题 + CTA按钮 */}
  </div>
  
  {/* 右侧: 视觉元素 (40%) ⚠️ 必须存在 */}
  <div className="flex-1 w-full max-w-lg lg:max-w-none">
    {/* 图标组合卡片 + 模块网格 + 关键指标 */}
  </div>
</div>
```

**右侧视觉元素必须包含**：
- [ ] 主图标（使用行业对应的 Lucide 图标）
- [ ] 模块网格（3x2 或 2x3 布局，展示核心能力）
- [ ] 关键指标（3个数据指标）

### 7.3 快速自检命令

// turbo
```bash
# 检查 Hero.tsx 是否包含右侧布局
grep -c "flex-1.*max-w" src/app/\(frontend\)/solution/industry/[slug]/Hero.tsx
```

**期望结果**: 返回 `1` 或更大的数字，表示包含右侧布局

### 7.4 常见遗漏清单

| 组件 | 常见遗漏 | 检查方法 |
|------|----------|----------|
| `Hero.tsx` | 右侧图标区域 | 检查是否有第二个 `flex-1` 子元素 |
| `IndustryChallenges.tsx` | 数据点 (dataPoint) | 检查每个挑战是否有业务数据支撑 |
| `KeyScenarios.tsx` | Tab 切换交互 | 检查是否使用 `useState` + `AnimatePresence` |
| `IndustryCases.tsx` | 客户引言 (quote) | 检查是否有 `quote` 字段 |
| `CTASection.tsx` | DemoRequestModal | 检查是否集成演示预约弹窗 |

---

## 快速参考

**SEO Metadata**：
```tsx
export const metadata: Metadata = {
  title: `${industryName}解决方案 - 数字化转型 | 泊冉软件`,
  description: `泊冉软件为${industryName}提供一站式数字化解决方案...`,
}
```

**CTA 按钮**：
```tsx
// 主CTA: 红色
<button className="bg-[#E60012] text-white">预约行业专家咨询</button>
// 次CTA: 蓝色边框
<button className="border-2 border-[#0052D9] text-[#0052D9]">下载白皮书</button>
```

**色彩比例**：70% 白/灰 + 20% 蓝 + 10% 红

**行业图标映射（扩展）**：
| 行业 | 图标 |
|------|------|
| 制造业 | `Factory` |
| 零售 | `ShoppingBag` |
| 医药 | `Pill` |
| 化工 | `Beaker` / `FlaskConical` |
| 乳制品 | `Milk` |
| 酒类 | `Wine` |
| 餐饮 | `UtensilsCrossed` / `Utensils` |
| 半导体 | `Cpu` |
| 互联网 | `Globe` |
| 国企 | `Building2` |
| 化妆品 | `Sparkles` |
| 科技服务 | `Code2` |
| 商务服务 | `Briefcase` |
| 能源 | `Zap` |
| 新材料 | `Atom` |

