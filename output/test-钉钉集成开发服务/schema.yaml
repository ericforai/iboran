schema_version: 1
page:
  keyword: "钉钉集成开发服务"
modules:
  - id: direct_answer
    h2: "一句话结论"
    required_fields:
      - "kb.direct_answer_text"
    format: "h2+p"
  - id: api_integration
    h2: "API 对接方案"
    required_fields:
      - "kb.api_integration_steps"
    format: "h2+steps"
  - id: message_push
    h2: "消息推送机制"
    required_fields:
      - "kb.message_push_methods"
    format: "h2+list"
  - id: approval_workflow
    h2: "审批流程设计"
    required_fields:
      - "kb.workflow_design_steps"
    format: "h2+steps"
  - id: data_sync
    h2: "数据同步策略"
    required_fields:
      - "kb.sync_strategies"
    format: "h2+list"
  - id: faq
    h2: "常见问题"
    required_fields:
      - "kb.faq_items"
    format: "h2+qa"

# Outline (comment only):
# - H1: 钉钉集成开发服务
# - H2: 一句话结论
# - H2: API 对接方案
# - H2: 消息推送机制
# - H2: 审批流程设计
# - H2: 数据同步策略
# - H2: 常见问题